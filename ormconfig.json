{
  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "clean-node-api",
  "synchronize": true,
  "logging": true,
  "entities": [
    "./src/infra/typeorm/entities/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/infra/typeorm/migrations"
  }
}
