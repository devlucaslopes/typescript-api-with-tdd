{
  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "clean-typescript-api",
  "synchronize": true,
  "logging": false,
  "entities": [
    "./src/infra/typeorm/entities/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/infra/typeorm/migrations"
  }
}
